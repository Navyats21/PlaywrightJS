# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.setup.js >> login once
- Location: tests/auth.setup.js:4:1

# Error details

```
Error: Error reading storage state from ../cookies.json:
ENOENT: no such file or directory, open '../cookies.json'
```

# Test source

```ts
  1  | const { test: setup, expect } = require('@playwright/test');
  2  | import { LoginPage } from "../Pages/LoginPage";
  3  | 
  4  | setup('login once', async ({ browser }) => {
> 5  |   const context = await browser.newContext();
     |                   ^ Error: Error reading storage state from ../cookies.json:
  6  |     const page = await context.newPage();
  7  |     const Login =  new LoginPage(page);
  8  |     await Login.launchUrl(Data.Url); 
  9  |     await Login.login(Data.Username,Data.Password);
  10 | });
```